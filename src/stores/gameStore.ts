// import { create } from 'zustand';
// import { stateMiddlewares } from './middlewares';
// import { type GameSlice, createGameSlice } from './slices/gameSlice';
// import { type GuestSlice, createGuestSlice } from './slices/guestSlice';

// export const useGameStore = create(stateMiddlewares<[GameSlice & GuestSlice]>((...args) => ({
//     ...createGameSlice(...args),
//     ...createGuestSlice(...args),
// })))

